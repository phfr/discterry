# Install torch + torch-geometric with CUDA in WSL to match your driver (see PyTorch / PyG wheel finder).
torch>=2.1
torch-geometric>=2.4
geoopt>=0.5.0
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
tqdm>=4.66
pyarrow>=14.0
# Optional: pip install "umap-learn>=0.5" for scripts/project_ball_umap.py
